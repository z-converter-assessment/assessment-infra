RABBITMQ_USER={{ vault_mq_user }}
RABBITMQ_PASS={{ vault_mq_password }}

# agent worker(consume + task.result publish)용 자격.
# ADR-0009 — 지금은 publisher 자격 재사용, vault에 worker 항목 분리 시 본 줄만 갱신.
RABBITMQ_WORKER_USER={{ vault_mq_user }}
RABBITMQ_WORKER_PASS={{ vault_mq_password }}
