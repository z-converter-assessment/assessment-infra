- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart assessment-agent
  ansible.builtin.systemd:
    name: "{{ agent_service_name }}"
    state: restarted
  listen: restart assessment-agent
