- name: AI VM — Ollama LLM 데몬
  hosts: ai
  become: true
  # diagnostic-worker는 engine compose 스택에서 실행(ADR-0010 단일 노드 compose).
  # AI VM은 Ollama 데몬만 호스팅하고, engine VM의 diagnostic-worker가
  # OLLAMA_BASE_URL로 본 VM에 원격 접속한다. (이전 venv+systemd diagnostic 제거)
  roles:
    - ollama
