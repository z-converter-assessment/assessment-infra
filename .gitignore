# Terraform
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/terraform.tfvars
*.tfvars
!*.tfvars.example

# Ansible — generated/secret
**/ansible/inventory.yml
# vault.yml은 ansible-vault(AES256) 암호화 후 commit — CD checkout이 워크스페이스로 확보 (ADR-0011·CLAUDE.md).
# ~/.vault-pass만 bastion 로컬. agent vault.yml은 engine vault.yml을 가리키는 symlink.

# 배포 artifact — 운영자가 bastion에서 다운로드 후 복사
engine/ansible/files/wheels/*.whl
engine/ansible/files/wheels/SHA256SUMS
agent/ansible/files/binaries/assessment-agent-linux
agent/ansible/files/binaries/assessment-agent.exe
engine/ansible/files/ollama/ollama-models.tar.gz

# OS
.DS_Store

# jetBrain idea
.idea/
