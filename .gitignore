# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
*.tfvars
!*.tfvars.example

# Ansible
ansible/inventory.yml
ansible/group_vars/all/vault.yml

# wheel 배포 파일 — 운영자가 배포 시 ansible/files/wheels/ 에 직접 복사
ansible/files/wheels/*.whl
ansible/files/wheels/SHA256SUMS

# OS
.DS_Store

# jetBrain idea
.idea/
