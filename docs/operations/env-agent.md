# Agent 환경변수 카탈로그

> assessment-agent에 주입되는 환경변수의 정식 카탈로그는 **`agent-test-environment.md`**(§3 주입되는 환경변수)로 통합됨.
> 그 문서는 환경변수뿐 아니라 fleet 구성·주입 경로(Linux EnvironmentFile / Windows win_environment)·실행 권한까지 단일 출처로 다룬다.
>
> engine 환경변수는 `env-engine.md` 참조.
